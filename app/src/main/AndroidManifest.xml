<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ssm.hel_per">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/helper_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".Intro"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".SignInActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".Survey"
            android:label="YourActivity"
            android:theme="@style/Theme.AppCompat.Translucent" />
        <activity android:name=".Main2Activity" />
        <activity
            android:name=".exercise_cycle"
            android:label="@string/title_activity_exercise_cycle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".exercise_buffet" />
        <activity android:name=".exercise_jumpingrope" />
        <activity android:name=".exercise_flank" />
        <activity android:name=".exercise_running" />
        <activity android:name=".CustomfoodActivity" />
        <activity android:name=".FoodselectActivity"></activity>
    </application>

</manifest>