<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <ImageView
        android:id="@+id/skyImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

    <RelativeLayout
        android:id="@+id/skylayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/buffet"
        >

        <ImageView
            android:id="@+id/swingImage"
            android:layout_width="180dp"
            android:layout_height="320dp"
            android:layout_centerHorizontal="true"
            android:background="@drawable/android_swing" />

        <TextView
            android:id="@+id/text_buffet"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/swingImage"
            android:gravity="center"
            android:text="Timer"
            android:textSize="24pt" />
    </RelativeLayout>

    <Button
        android:id="@+id/nextbtn_buffet"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/skylayout"
        android:layout_toEndOf="@+id/buffet"
        android:text="다음"
        android:textSize="60px"/>


    <ImageView
        android:id="@+id/buffet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_weight="1"
        app:srcCompat="@drawable/buffet" />

</RelativeLayout>