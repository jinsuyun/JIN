<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".exercise_all.exercise_standard.standard_lv1.exercise_handstand_pushup"
    tools:showIn="@layout/activity_exercise_handstand_pushup">

    <ImageView
        android:id="@+id/skyImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="400dp"
        >

        <ImageView
            android:id="@+id/swingImage"
            android:layout_width="180dp"
            android:layout_height="320dp"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:background="@drawable/android_swing" />
    </RelativeLayout>

    <Button
        android:id="@+id/nextbtn_handstand_pushup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_handstand_pushup"
        android:layout_toEndOf="@+id/handstand_pushup"
        android:text="다음" />

    <TextView
        android:id="@+id/text_handstand_pushup"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/handstand_pushup"
        android:layout_toEndOf="@+id/handstand_pushup"
        android:text="Timer"
        android:textSize="20dp" />

    <ImageView
        android:id="@+id/handstand_pushup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_weight="1"
        app:srcCompat="@drawable/handstand_pushup" />

</RelativeLayout>